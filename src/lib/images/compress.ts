/**
 * @deprecated Import from compress-client (browser) or compress-server (API) instead.
 * Kept for compatibility — client-safe only (no sharp).
 */
export {
  IMAGE_LIMITS,
  compressImageClient,
} from "./compress-client";
